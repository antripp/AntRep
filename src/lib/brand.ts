import type { Role } from "./types";

/** Portal mascots (sign-in + in-app). PWA icons stay in /icons/. */
export const MASCOTS: Record<Role, string> = {
  athlete: "/mascots/athlete-mascot.png",
  coach: "/mascots/coach-mascot.png",
};
