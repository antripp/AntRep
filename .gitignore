node_modules
dist
.env
.env.local
.env.*
!.env.example
.DS_Store
*.local
*.tsbuildinfo

# Editor / AI tooling (local only)
.claude/
.cursor/

# Database SQL — run manually in Supabase SQL Editor.
# Migrations are versioned so schema changes travel with the code; the rest
# (schema.sql, hotfixes, seed data) stays local.
supabase/*
!supabase/.gitkeep
!supabase/migrations/
supabase/migrations/*
!supabase/migrations/*.sql
